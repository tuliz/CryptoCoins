import styled from '@emotion/styled';

const Div = styled.div`

`;

const Favorites = ()=>{
    return(
        <Div>
            <h2>My Favorites:</h2>
        </Div>
    );
}

export default Favorites;